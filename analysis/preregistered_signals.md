# Pre-registered thesis signals — confirm/kill criteria

> **⚓ Frame — read first (reanchor):** this repo is a **general forensic engine for financial fragility & mispricing across the _whole economy_** — hidden / mismatched / circular leverage and valuation-run-ahead-of-cash-flow, found with **no sector prior**. **AI / data-center is _case zero_, not the object** (the method is sector- and era-agnostic — cf. fiber 1999, shale 2014). **Operating mode: maximum exhaustiveness — broad AND deep, uncapped, ≥ $1M-substance floor, US-primary (international by connectedness-to-core × data-accessibility); acting resource-constrained is NEVER correct; the only stop is physics (data that doesn't publicly exist → estimate it from proxies).** Full scope & doctrine: [total_ecosystem_dive.md](total_ecosystem_dive.md) · [README](../README.md).


**Registered: 2026-06-12.** These are the dated, observable conditions under which the market-facing thesis (see [expectations_vs_measured.md](expectations_vs_measured.md)) is **confirmed** or **killed** — including the conditions under which *our own timing is wrong*. Registration discipline: thresholds are set *before* outcomes are known and may not be silently edited. Amendments are append-only in the log at the bottom, with reasons.

**Scope guard:** these signals govern the *market-facing thesis* (mispricing + timing). The engine's adjudicated balance-sheet measurements (coverage breaches, duration mismatch, maturity wall, who-holds) are filings-derived facts and are not killable by price action — only by corrected filings or corrected extraction.

## Semantics

- **confirming** — evidence the marginal buyer of cluster credit is cracking (the thesis's required transmission).
- **contra** — evidence the funding window is open (the timing-kill direction).
- **neutral** — between thresholds.

Quantitative components (S1, S1b, S2′, S3′, S4) are auto-evaluated **hourly** by `scripts/refresh_live_overlay.py` (logic in `src/bubble/market_signals.py`, unit-tested) into `viz/live.json` (`signals`) and surfaced on the explorer's **credit** chip and the banner's credit dial. Compound criteria (below) are adjudicated by a human against those components — the script measures, it does not declare victory.

## Auto-evaluated components (as amended 2026-06-12 — see log)

| id | measure | confirming at | contra at | threshold derivation (registered values) |
|---|---|---|---|---|
| **S1_new_issue_spread** | latest hand-carded cluster new-issue print ([issuance_cards.json](issuance_cards.json)), coupon vs 5y UST (FRED DGS5) | ≥ **800bp** | ≤ **600bp** | At registration the cluster cleared ~330bp (SPV, Jun-2) to ~575bp (corporate 9.75% '31): ≤600bp means the window is demonstrably open at today's levels. ICE's distress convention is ~1000bp OAS; 800bp is the pre-distress band between today's prints and distress. **Staleness guard (A4):** a print > **45 days** old reports `stale`, never `contra` — absence of issuance is ambiguous (window shut OR no need); 45d exceeds the longest gap between carded prints at amendment time (38d). |
| **S1b_failed_print** (A3) | status of every carded cluster print (`priced\|postponed\|pulled\|failed` in [issuance_cards.json](issuance_cards.json)) | any non-priced print (discrete event) | n/a (all priced = neutral) | Issuance windows historically gap shut rather than taper ('99 vendor financing, '07 ABS): the first failed/pulled cluster print is confirming regardless of spread drift on completed deals. |
| **S2_ccc_divergence** (respecified A2) | **(CCC − HY) OAS differential, YTD change** (FRED BAMLH0A3HYC − BAMLH0A0HYM2), with BB OAS (BAMLH0A1HYBB) as quality-end control | ≥ **+1.5pp** YTD | ≤ **0pp** YTD | Same 2x-observed derivation as the original (differential was +0.73pp YTD at amendment), but now measured as a true differential so a parallel HY selloff can't fire it. **Market-wide flag:** BB YTD ≥ **+1.0pp** (BB sat at 1.69%, −0.02 YTD, at amendment — +1.0pp on that base is a major broad-HY event) marks the move as possibly market-wide; flag, adjudicator weighs. |
| **S3_bdc_discount_differential** (respecified A1) | worst **AI-exposed** listed-BDC discount to reported NAV (BXSL, ARCC) **minus** non-AI control median (MAIN, GBDC, TSLX, PSEC) — membership carded in [bdc_exposure_cards.md](bdc_exposure_cards.md) | ≥ **+15pp** | ≤ **+5pp** | Normal-times BDC discounts run 0–10% sector-wide and ~5pp cross-sectional dispersion is routine, so a worst-exposed name ≥15pp wider than the contemporaneous non-AI median (~1.5x the entire normal-times band) is exposure-specific stress; ≤5pp is inside ordinary dispersion. Median control absorbs the regime outliers (MAIN premium, PSEC idiosyncratic discount). At amendment: BXSL 9.2% − control median 1.7% = **+7.5pp → neutral** (overlay-measured). |
| **S4_demand_trajectory** (A5) | trailing YoY growth of the carded AI end-demand basket ([demand_trajectory.json](demand_trajectory.json): OpenAI + Anthropic + Microsoft-AI company-stated run-rates; growth-not-level semantics) | < **+50%**/yr | ≥ **+100%**/yr | The capex-implied revenue requirement (~$500B/yr on book lives by ~2028-29, from ~$100–150B/yr observable mid-2026) needs ~70–100%/yr compounded: below 50%/yr the demand leg cannot close even the generous (book-life) equation on schedule; ≥100%/yr is the bull path intact. At registration: **+253% YoY → contra** (the bull's strongest current number, registered anyway). |

## Compound criteria (human-adjudicated, dated)

- **TIMING-KILL** — *our crack-window timing is wrong*: through **2026-Q4**, every new cluster 5yr+ print clears ≤600bp with >2x books (S1 stays contra — a `stale` S1 does **not** count toward the kill, per A4) **and** S3′ reads contra (exposed-vs-control differential ≤5pp; original wording "all top-4 discounts <10%" translated by A1). Consequence: the 2025-Q3..2027-Q3 crack window is killed; the balance-sheet claims stand; the wall analysis shifts weight to 2030–33.
- **MARGINAL-BUYER-KILL** — *the funding-chain stress read is wrong*: private-credit redemption pressure abates in trade-press coverage for two consecutive quarters and S2′ returns ≤0 while issuance volume continues. Consequence: layer-3 of the pricing map is withdrawn.
- **CONFIRM-1 (window cracking)** — any cluster deal pulled/postponed (= S1b confirming), or a print ≥800bp (S1 confirming), while maturities or DDTL drawdowns are pending.
- **CONFIRM-2 (transmission)** — S2′ ≥ +1.5pp (without the market-wide flag) **and** S3′ ≥ +15pp simultaneously: fund-flow stress reaching price-of-risk for the tail, attributably. (Original wording used the pre-amendment S2/S3; translated by A1/A2.)
- **CONFIRM-3 (issuer event)** — any cluster issuer discloses a going-concern qualifier, covenant waiver request, or distressed exchange. (Filing-tier; would also feed the adjudicated engine.)

## Calendar of forced truth (manual carding)

| date | event | why it forces truth |
|---|---|---|
| each 10-Q/K season | issuer depreciation-schedule changes; utilization/coverage disclosures | book life vs ~2–3yr economic life is the contested number |
| quarterly, ~May/Aug/Nov/Feb | re-card BDC NAVs from Q results into `scripts/refresh_live_overlay.py` (`BDC_NAV`) | keeps S3 honest — close vs *reported* NAV, never estimated |
| every cluster print | append to [issuance_cards.json](issuance_cards.json) (date, coupon, tenor, size, books if reported) | keeps S1 honest and builds the spread time series |
| 2026-Q4 | TIMING-KILL adjudication date | pre-committed above |
| 2027–2028 | anchor-contract renewal/extension checkpoints | backlog quality becomes observable |
| 2029–2033 | carded maturity wall ($36.6B of $41.7B carded, 88%, in 2030–33) | refinancing vs collateral age — the terminal test |
| Oct-2030 | CoreWeave–OpenAI commitment expiry | the contract servicing the wall is itself up for renewal |

## Amendment log

- 2026-06-12 — initial registration (S1 ≥800/≤600bp; S2 = CCC OAS YTD ≥+1.5pp/≤0pp; S3 = worst top-4 {OBDC, ARCC, BXSL, FSK} discount ≥30%/all <10%; S3 noted as confirming-on-registration via FSK).
- 2026-06-12 — **A1: S3 respecified to a differential (the material fix).** Reason: carding each book ([bdc_exposure_cards.md](bdc_exposure_cards.md)) showed the original proxy was confounded — FSK's −41% is dominated by legacy-book credit, a class action, and a years-long pre-AI discount (86% of its book internally rated low-AI-risk); OBDC's AI-DC lending sits at the *manager* level, not in the listed fund; only BXSL (and weakly ARCC) shows asset-side AI/DC exposure. New spec: S3′ = worst exposed-set discount − non-AI control median; thresholds ≥+15pp confirming / ≤+5pp contra (derivation in table). **Effect: the registration-day "confirming" is withdrawn — S3′ read neutral (+7.5pp) at amendment.** Compound criteria translated accordingly. Honest residual: direct neocloud lending sits in private funds, so even S3′ is a weak window on the actual marginal buyer; carded confounds listed in the exposure cards.
- 2026-06-12 — **A2: S2 respecified to the (CCC − HY) differential with a BB control.** Reason: the original measured CCC level change alone, so a parallel market-wide HY selloff would have fired a "tail divergence" signal. Same threshold magnitudes (derivation logic unchanged; differential +0.73pp YTD at amendment = neutral, same reading as before the respec). BB OAS (BAMLH0A1HYBB) added as the quality-end control with a ≥+1.0pp market-wide flag. CCC-ex-cluster would be the ideal numerator and is not constructible keyless; documented limitation.
- 2026-06-12 — **A3: S1b registered (discrete failed-print event).** Reason: issuance windows historically gap shut rather than taper; spread drift on *completed* deals can never capture a print that doesn't happen. `issuance_cards.json` gains a `status` field; all three existing cards are retro-stamped `priced` (they were completed prints — no information change).
- 2026-06-12 — **A4: S1 staleness guard.** Reason: S1 evaluates the *latest carded print*, so a long issuance silence would have left it frozen on a months-old "contra" reading — treating absence of issuance as evidence the window is open, which it is not (shut window and no-need-to-issue are observationally similar here). >45 days → `stale` (45d > the 38d longest observed gap between carded prints); `stale` never counts toward TIMING-KILL.
- 2026-06-12 — **A5: S4 registered (demand trajectory).** Reason: the bull memo's load-bearing claim (demand doubles into the capacity) had no pre-registered measurement, and the basket definition is registered *now* precisely so a hostile bull disputes the trajectory, not the baseline. Basket = company-stated run-rate prints only (OpenAI, Anthropic, Microsoft-AI; composition fixed; growth-not-level semantics; double-count caveats carded in [demand_trajectory.json](demand_trajectory.json)). Thresholds <+50%/yr confirming / ≥+100%/yr contra (derivation in table). **Registered reading: +253% YoY = contra — the strongest current number on the bull side, registered without flinching.** Year-ago baseline is retro-carded sourced measurement (the thresholds are ex-ante; the baseline is history), weakest-sourcing flags carded.
