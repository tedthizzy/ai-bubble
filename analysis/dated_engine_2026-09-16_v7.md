# Dated engine run: 2026-09-16

Customer demand is measurable and the issuer panel remains cash-investment-intensive. The selected QTS print makes registered S1 contra while one postponed and one abandoned bond attempt make S1b confirming. S2 confirms weaker tail credit while S3 is contra, so CONFIRM-2 is false. Current evidence does not resolve the timing or probability of a system-wide unwind.

The unchanged registered functions return S1 `contra` at 180 basis points, S1b `confirming`, and S2 `confirming` at +2.04 percentage points and S3 `contra` at +1.5 points. CONFIRM-2 is `False`. S1 uses the registered coupon-minus-five-year-Treasury proxy: QTS's 6.625% coupon minus the September 15 Treasury 4.83% gives 180 basis points. QTS closed August 27; its public issue price and yield were not verified, and the selected card universe is incomplete. Bloomberg-reported Prime and Pure bond withdrawals support S1b; Pure separately obtained bank financing. S4 lacks a new comparable company-stated basket. No September bubble probability was generated.

The SEC XBRL panel checked 22 entities. Among 13 issuer-labeled panel entries, 11 of 11 with matched 2026 cash-flow/capex periods had negative cash after capex. 1 additional matched issuer observation ended before 2026. This preselected cohort is not a sector prevalence estimate.

The production physical-capacity function read 16,515 queue records, 2,728 tracker projects, and 105,344 equipment rows. Its 531,011.143 MW is gross generation interconnection requests, not energized AI load. The data-center classifier covers 11,717.7 MW of queue rows; it is not incremental to the gross queue total.

The production compute-economics function read 87 September listed GPU quotes. 45 comparable June/September quotes had 12 increases, 1 decrease, and 32 unchanged. It has 0 dated project payback cases and 0 dated depreciation policies, so it cannot compute a current project payback or book-life gap.

The non-SEC catalog acquired 483 of 484 attempted sources. The current SEC document inventory contains 41,589 rows. Its coverage status is `exact_url_coverage_audited`; see the JSON for the exact URL-reconciliation totals. The capital graph state is FERC `input_sizes_match_manifest`, EDGAR-joined `not_run`, and GLEIF ownership `input_sizes_match_manifest`. Pending contract rows and graph edges are not additive debt.

The legacy final report's June handoff-based verdict, contagion graph, and hardcoded customer concentration were not executed or relabeled as September results.

Exact input paths and SHA-256 hashes, per-issuer values, signal statuses, and graph coverage are in the adjacent JSON. This run is an exact URL audit of the saved inventory; it does not claim complete SEC coverage. Use a new `--output-prefix` after additional SEC documents or Feed days are acquired.
