# SEC acquisition status, September 16, 2026

This is the current SEC evidence state for the September 16 refresh. It records the measured denominator, the saved direct-document corpus, the Feed experiment, and the remaining public-source boundary.

The dated Q2 and Q3 official master indexes cover June 1 through September 15. They contain 367,387 raw index rows, 367,371 unique CIK-accession pairs, and 257,675 unique accessions. Sixteen repeated CIK-accession rows are retained because the SEC indexed one accession under more than one form label. The September 16 daily index was not present in the saved source set; a direct check at 22:22 CDT returned HTTP 403, so September 16 filings are not asserted absent. The local master coverage audit records `requested_end_date_observed: false`.

The expanded selected manifest contains 163,063 primary rows for 8,060 selected CIKs. It covers 7,162 CIKs with selected filings. Against the official index, all 161,388 official filing pairs for selected CIKs overlap the final selected manifest, while 205,983 official filing pairs belong to 68,455 CIKs outside that selected set. Exact name matches outside the selected CIK set are preserved as 6,728 leads; they are not treated as verified AI exposures. The denominator and selection decisions are in [the master-index audit](sec_master_index_coverage_2026-09-16.md) and [the saved coverage JSON](../data/sec_master_index_2026-09-16/coverage_2026-06-01_to_2026-09-16_v2.json).

The direct acquisition run attempted 35,338 documents and saved 34,614. The merged inventory contains 41,589 rows because it retains earlier FWP and exhibit work. Every one of the 724 recorded request errors is an SEC HTTP 429 response. The inventory itself has no duplicate URLs and no rows with an embedded error. The current inventory is therefore a verified saved-document corpus, not a complete selected-manifest census.

The exact URL audit against the final selected primary manifest plus the offline exhibit manifest requested 180,044 URLs. It found 37,706 in the inventory, 142,338 missing, and 724 known request errors. The 3,883 inventory URLs outside those manifests are retained and flagged; they do not silently increase the denominator. The audit means URL presence only. The offline re-extraction verifies the local bytes separately.

The SEC Feed route is viable but incomplete. A whole June 10 archive was downloaded and its gzip trailer and tar structure verified. It contained 2,944 unique Feed accessions and 2,128 selected document payloads, including 561 exhibits. The archive had 910,920,768 compressed bytes and SHA-256 `8ed831798e3f2e490b8053a98940b9d1065972a5282f1f9f909923ec2d6838e1`. Same-day master reconciliation was false: 49 master accessions were absent from the Feed and 106 Feed accessions were absent from that day's master index. Those residuals are preserved in the pilot output. A full 74-day Feed transfer was stopped when SEC rate limiting returned HTTP 429; no global Feed completion claim is made.

The parser correction is applied offline to saved bytes. The prior 6,975-document diagnostic verified every source hash and changed 478 maturity fields: 229 became blank and 249 changed to another date. Exactly 150 dates previously labeled 2026 became blank. The changed values were prospectus-expiry text falsely interpreted as debt maturity; deal IDs, amounts, classifications, and other content fields were unchanged. The completed current pass verified all 41,589 inventory documents and 14,477,363,465 raw bytes with no network calls or hash mismatches. It produced 27,759 deal candidates and 6,116 tranche candidates in [the first corrected output](../data/edgar_reextracted_current_inventory_2026-09-16/offline_reextraction.summary.json). The final metadata-preserving output is [here](../data/edgar_reextracted_current_inventory_2026-09-16_v2/offline_reextraction.summary.json); its deals and tranches hashes are `5916784a60190ae3e4549aa2d4f42e3762be89ed750e3ab4d7dae590dbffc2c0` and `c8c0481a24b75e1f15b9b17fcb885a2230f27a0f751b0fba53517759b1f1a2ae`.

The metadata-preserving rerun used a 183,927-row union manifest (selected primary and exhibit URLs plus the retained extras; SHA-256 `b11249b9bca0c9f76aa967616b7bd85c5487539ff013b3f6e2ab1d7de5879bbf`) and matched metadata for all 41,589 inventory rows. Relative to the prior current deal table, it keeps all 27,759 deal IDs, changes 2,412 maturity values and the associated 404 confidence fields, and changes 2,056 exhibit title descriptions. The title changes only remove or restore the literal `SEC exhibit` description prefix; no deal identifier or source hash changes.

SEC fair-access policy, unavailable September 16 index data, private placement terms, and the incomplete Feed route are the remaining coverage limits. They are evidence boundaries, not evidence of no filing or no exposure. The dated engine uses the exact URL audit and reports these residuals in [its JSON](dated_engine_2026-09-16_v7.json).

The repository-wide provenance audit then scanned 49 CSV outputs and 13,094,234 rows, checked 6,864,373 source-URI values, and found zero violations and zero warnings in [the final audit](../data/reports/source_invariant_audit_2026-09-16_v3.json). The earlier v2 audit was captured during a concurrent derived-file write and is retained as an immutable diagnostic; v3 is the completed read after all writers closed.
